import { OptionsScreen } from './OptionsScreen';

// Perfil absorbe Opciones (tema, haptics, updater). La grilla de progreso SRS
// se agrega cuando exista el store (ver HANDOFF fases 4-5).
export function ProfileScreen() {
  return <OptionsScreen />;
}
